import type { Metadata } from "next";
export const metadata: Metadata = { title: "设置", description: "管理 AI 接口与 SALINGO 本地学习数据。" };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
