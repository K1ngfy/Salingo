import type { Metadata } from "next";
export const metadata: Metadata = { title: "学习统计", description: "查看 CISSP 学习趋势、薄弱域与模考记录。" };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
