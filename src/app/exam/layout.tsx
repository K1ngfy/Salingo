import type { Metadata } from "next";
export const metadata: Metadata = { title: "全真模考", description: "CISSP 多域计时模考与分域成绩报告。" };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
