# SALINGO 功能自检清单

检查日期：2026-07-15

## 自动检查

- [x] `npm audit`：0 个已知漏洞。
- [x] `npm run typecheck`：严格 TypeScript 检查通过。
- [x] `npm run check:bank`：800 道题、800 个唯一 ID、八域各 100 道、结构与答案引用有效。
- [x] `npm run build`：Next.js 15 生产构建与 `output: "export"` 静态导出通过。

## 页面与交互

- [x] 首页八域权重与现行 ISC² Exam Outline 一致（16/10/13/13/13/12/13/10）。
- [x] 桌面顶部导航和移动端底部导航均可访问全部核心页面。
- [x] 390×844 移动视口无横向溢出，答题与解析面板可正常滚动。
- [x] 单选和多选可选择、判分并显示正确/错误状态。
- [x] 错误作答触发红色反馈、四段式解析并写入 FSRS 队列。
- [x] AI 深度解析缺少 Key 或接口失败时显示行内提示，内置解析保持可用。
- [x] 每域提供 10 节考点学习地图，并可进入覆盖不同蓝本的 10 题闯关。
- [x] 正确作答触发绿色反馈并更新答题统计。
- [x] 页面刷新后答题记录、学习日期和复习卡片仍保留。
- [x] 题库支持域筛选、关键词搜索、展开解析、JSON 导入导出。
- [x] AI Key 缺失和接口失败显示行内错误；不出现白屏或无限等待。
- [x] 模考支持域、题量、时间配置；题目不足时明确降级为全部可用题且不重复。
- [x] 模考支持暂停、标记、答题卡跳转、统一交卷、分域雷达图和错题归集。
- [x] 统计页面显示近 14 天趋势、薄弱域和模考记录。
- [x] 完整数据备份、恢复、二次确认重置可用。
- [x] 404、键盘焦点、跳转主内容和减少动画偏好已实现。

## 边界核对

- [x] 无登录、注册、账户、权限或 JWT。
- [x] 无独立后端、云数据库、接口服务或云同步。
- [x] 无付费、会员、广告、分享、社交排行榜或数据上报。
- [x] AI 仅由浏览器直连用户配置的 OpenAI 兼容端点。
- [x] 内置题目均标记 `original` 与 `2024-current`，未包含商业题库或真实考试题原文。
